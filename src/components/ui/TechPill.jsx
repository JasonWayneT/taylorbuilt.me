export default function TechPill({ label }) {
  return (
    <span className="px-3 py-1 bg-gray-100 text-text-secondary text-xs font-semibold rounded-full border border-gray-200">
      {label}
    </span>
  );
}
